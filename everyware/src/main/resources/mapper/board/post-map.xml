<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.board.post-map">
<!-- 전체조회 -->
<select id="selectPostAll" resultType="PostVO">
	 SELECT post_id,
	 		board_id,
	        notuification_yn,
	        hold_yn,
	        title,
	        write_date,
	        view_cnt,
	        recommend_cnt,
	        attachment_group_id,
	        content,
	        emp_id
	    FROM post
</select>
</mapper>