<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yedam.app.dataroom.dataroom.mapper.DataMapper">
	
	<!-- 전체 조회 -->
	<select id="selectDataList" resultType="DataVO" parameterType="DataVO">
		SELECT * 
		FROM (SELECT rownum rs, a.* 
		      FROM (SELECT data_id
		                , title
		                , registrate_date
		                , attachment_group_id
		                , emp_id
		                , department_id
		                , remarks
		            FROM data_room
		            <include refid="dataroomCategory"></include>
		            ORDER BY data_id DESC) a 
		     WHERE rownum <![CDATA[<= ]]> (#{page}) * (#{cnt})) 
		WHERE rs > (#{page}-1) * (#{cnt})
	</select>
	
	<!-- 전체조회 cnt -->
	<select id="cntDataList" resultType="int">
		SELECT count(*)
		FROM data_room
		<include refid="dataroomCategory"></include>
	</select>
	
	<!-- category sql -->
	<sql id="dataroomCategory">
        <choose>
          <when test="category == 'common'">
            WHERE remarks = #{remarks}
          </when>
          <when test="category == 'dept'">
            WHERE department_id = #{dataVO.departmentId} and remarks = #{remarks}
          </when>
          <when test="category == 'private'">
            WHERE emp_id = #{dataVO.empId} and remarks = #{remarks}
          </when>
       </choose>
 	</sql>
 	
 	<!-- empId로 DeptId 조회 -->
	<select id="selectDeptId" resultType="String">
		SELECT department_id
		FROM emp
		WHERE emp_id = #{empId}
	</select>
	
	<!-- ===== 자료등록 ===== -->
	<insert id="insertData" parameterType="DataVO">
		<selectKey keyProperty="dataId" resultType="int"
			order="BEFORE">
			SELECT DATA_ID_SEQ.NEXTVAL
			FROM dual
		</selectKey>
		INSERT INTO data_room (
				data_id
			  , department_id
			  <if test="attachmentGroupId != null and attachmentGroupId != ''">
			  , attachment_group_id
			  </if>
			  , emp_id
			  , title
			  , remarks		
			 )
			 VALUES(
			 	#{dataId}
			 	, #{departmentId}
			 	<if test="attachmentGroupId != null and attachmentGroupId != ''">
			 	, #{attachmentGroupId}
			 	</if>
			 	, #{empId}
			 	, #{title}
			 	, #{remarks}
			 )
	</insert>
	
	<!-- 자료 조회 -->
	<select id="selectData" resultType="DataVO">
		SELECT 
		     d.data_id 
		    , d.title
		    , d.registrate_date
		    , a.upload_path
		    , a.upload_file_name
		    , a.origin_file_name
		    , a.attachment_group_id
		    , a.file_size
		    , a.ext
		    , a.file_id
		FROM data_room d
		INNER JOIN attachment a
		ON d.attachment_group_id = a.attachment_group_id
		WHERE data_id = #{dataId}
	</select>
	
	<!-- 자료 삭제 -->
	<delete id="deleteData" parameterType="java.util.List">
		 DELETE FROM data_room
		 WHERE data_id IN 
		<foreach item="item" index="index" collection="list" 
				 open="(" separator="," close=")">
			#{item, jdbcType=INTEGER}
		</foreach>
	</delete>
	
	<!-- dataIds로 파일삭제 -->
 	<delete id="deleteFiles" parameterType="java.util.List">
 		DELETE FROM attachment
		WHERE attachment_group_id IN( 
		                SELECT attachment_group_id
		                FROM data_room
		                WHERE data_id IN 
	    <foreach item="dataIds" collection="dataIds" open="(" separator="," close=")">
               #{dataIds}
        </foreach>
        )
 	</delete>
 	
 	
</mapper>